use "sys"

pkg sys =
;;

const __init__ = {
	var sa, osa

	/* is there ever a time we care about sigpipe? */
	sa = [
		.handler = ({;} : byte#),
		.flags = sys.Sarestart,
		.mask = [.bits = [0,0,0,0]]
	]
	sys.sigaction(Sigpipe, &sa, &osa)
}
