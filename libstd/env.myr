use "sleq.use"

pkg std =
	const getenv	: (name : byte[:] -> option(byte[:]))
;;

extern var _environment	: byte[:][:]

const getenv = {
	var i
	var env

	for i = 0; i < _environment.len; i++
		if !sleq(
