use std
/* checks that matching integers works. exits with 84. */
const main = {
	var v

	v = 12
	match 12
	| 1:	std.exit(42)
	| 2:	std.exit(81)
	| 3:	std.exit(123)
	| 4:	std.exit(99)
	| 12:	std.exit(84)
	| 6:	std.exit(18)
	| x:	std.die("Got an unexpected int!\n")
	;;
}
