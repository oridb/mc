use std

const main = {args : byte[:][:]
	match getoption(123)
	`std.Some val:	std.put("Got a value: %i\n", val);;
	`std.None:	std.put("No value returned\n");;
	;;
}

const getoption = {val
	match val
	0:	-> `std.None;;
	_:	-> `std.Some val;;
	;;
}
