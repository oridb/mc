use std

/* checks that parameterized types work. exits with 0. */
type option(@a) = union
	`Some @a::(tcint,tcnum,tctest)
	`None
;;

const main = {
	var v

	v = `Some 123
	match v
	| `None:	-> 1
	| `Some 123:	-> 0
	;;
	-> 60
}
