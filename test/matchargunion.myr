use std
/* checks pattern matching on unions with arguments.
exits with 42. */
type u = union
	`Int int
	`Chr char
	`Nil
;;

const main = {
	var v

	v = `Int 123
	match v
	`Int 127:
		-> 42
		;;
	`Int 123:
		-> 69
		;;
	`Chr 'a':
		-> 4
		;;
	`Nil:
		-> 6
		;;
	;;
}

