use std
/* checks that we can bind values in pattern matches.
exits with 11. */
type u = union
	`Int int
	`Chr char
	`Nil
;;

const main = {
	var v

	v = `Int 8
	match v
	`Int 127:
		-> 42
		;;
	`Int x:
		-> x
		;;
	`Chr 'a':
		-> 4
		;;
	`Nil:
		-> 6
		;;
	;;
}

