use "sys.use"
use "alloc.use"

const main = {
	var x : byte*[1024]
	var i

	for i = 0; i < 1024; i++
		x[i] = std.bytealloc(32)
	;;
	for i = 0; i < 1024; i++
		std.bytefree(x[i], 32)
	;;
	std.write(1, "Hello world\n")
}
