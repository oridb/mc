use regex
use std

const main = {
	var found
	match regex.compile(".*^a*")
	`std.Success re:
		found = regex.exec(re, "b\naaa")
		std.put("Found = %t: len = %z\n", found, re.strp)
		-> 0
		;;
	`std.Failure err:
		std.put("failed to compile regex")
		-> 1
		;;
	;;
}

